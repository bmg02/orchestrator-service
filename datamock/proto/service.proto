syntax = "proto3";

package proto;

option go_package="./proto";

message Request {
    string Name = 1;
}

message Response {
    string Name = 1;
    string Class = 2;
    int64 Roll = 3;
}

service UserService {
  rpc GetUserMockData(Request) returns (Response);
}